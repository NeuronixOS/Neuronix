# Optional. Personalize overlay replaces this with named outputs.
# Do not addreserved a region just to park floats: Hyprland clamps xdg-popups
# (GTK/Chrome menus) to the remaining work area, so right-click menus vanish
# on windows in that reserved zone.
