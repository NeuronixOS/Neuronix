# Install desktop stack + Hyprland/Chrome when Desktop profile was chosen.
# Cursor is not stock — personalize/install/*.sh may add it via merge.
# Do not embed ${var} in inline bash — Calamares treats those as GlobalStorage keys.
---
dontChroot: false
timeout: 3600

"packagechooser_neuronix-profile":
  desktop:
    - command: "/usr/local/sbin/neuronix-apply-desktop-profile.sh"
    - command: "/usr/local/sbin/neuronix-install-hyprland-backports.sh"
    - command: "/usr/share/neuronix/install-google-chrome.sh"
  server:
    - command: "bash -c 'echo \"[neuronix] Server profile — skipping desktop install\"'"
