---
# After Calamares networkcfg: rewrite NM keyfile `permissions=user:live:` to the
# real installed user when networkcfg could not detect the live username (root utmp).
dontChroot: false
timeout: 30

script:
  - command: "/usr/local/sbin/neuronix-nm-user-perms.sh"
