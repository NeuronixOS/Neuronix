# Skip NVIDIA DKMS on anything but trixie 6.12 LTS.
# A failed autoinstall on linux 7.x breaks linux-image configure, so every
# later apt upgrade/install dies. Status 77 is a skip, not a failure.
BUILD_EXCLUSIVE_KERNEL="^6\.12"
