# LightDM must not start until live-config has created user "live" and set autologin.
# On installed disks live-config is inactive (ConditionKernelCommandLine=boot=live) — After= is fine.
[Unit]
After=live-config.service
Wants=live-config.service
