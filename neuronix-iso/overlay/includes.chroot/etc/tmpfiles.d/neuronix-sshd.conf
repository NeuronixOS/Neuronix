# Neuronix: OpenSSH privilege-separation directory on tmpfs (/run).
# Redundant with openssh-server's own tmpfiles.d when the package is intact;
# ensures /run/sshd exists after boot if that snippet is missing or overridden.
d /run/sshd 0755 root root -
