#!/bin/sh
# First boot on live ISO: ensure host keys exist before ssh.socket accepts connections.
set -e
if [ ! -f /etc/ssh/ssh_host_ed25519_key ] && [ ! -f /etc/ssh/ssh_host_rsa_key ]; then
	/usr/bin/ssh-keygen -A
fi
mkdir -p /run/sshd
chmod 0755 /run/sshd
/usr/sbin/sshd -t
