# personalize/configs/icons/

Cursor / icon theme drop-ins for the session. Usually only `default/` is needed
(→ `~/.icons/default` via `links.json`). See that subfolder’s README for an
`index.theme` example.
