# personalize/configs/icons/default/

Cursor theme for the session (Appearance / xsettingsd) → `~/.icons/default`.

## Example `index.theme`

```ini
[Icon Theme]
Name=Default
Comment=Default Cursor Theme
Inherits=Adwaita
```

Optional: `cursors/` directory with cursor files when shipping a custom theme.
