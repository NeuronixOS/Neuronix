# personalize/gtk-apps/gtk-theme/python/

Python modules that ship with the Neuronix GTK theme package. Overlay only files
you need to customize; leave empty to keep stock defaults.

## Example

```text
python/
  theme_utils.py     # shared helpers
  colors.py          # palette helpers
```
